# DEFAULT lane — the numpy-light pipeline deps for .venv-pipeline (local-only, never deployed). Kept numpy-only on
# purpose so CI + the clone-verify install instantly with no native build; the WofE/CI/LR algorithm itself is the
# TypeScript engine (frontend/src/mpm/), run by the bake via tsx. The heavy learned lane (torch -> ONNX) is pinned
# separately in data-pipeline/requirements-precompute.txt and documented in docs/frameworks/03_torch-onnx.md.
numpy==2.4.6
